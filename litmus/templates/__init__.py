"""Bundled project templates — scaffolds and the sample pipeline.

These ship inside the installed wheel so ``pipx install litmus-data``
users can run ``litmus`` in any directory and have the sample dataset
available without cloning the repo.
"""

from __future__ import annotations
